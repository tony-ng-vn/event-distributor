---
name: understanding
description: >-
  Deprecated name — use the global pr-explainers skill instead
  (~/.agents/skills/pr-explainers or .cursor/skills/pr-explainers).
  Kept so old prompts that say "understanding" still discover the redirect.
---

# understanding → pr-explainers

This skill was renamed to **`pr-explainers`** so it does not collide with
**Understand Anything** (`/understand` knowledge graphs).

**Use:** `pr-explainers` (global: `~/.agents/skills/pr-explainers/SKILL.md`,
also linked at `.cursor/skills/pr-explainers` in this repo).

Artifact paths are unchanged: `docs/understanding/`.
